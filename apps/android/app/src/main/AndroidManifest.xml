<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!--
        Installing an update the app downloaded itself. There is no store here:
        this is sideloaded onto a phone that sits in a drawer, so nothing else
        will ever update it. Android still asks the user to allow it, and then
        confirms the install separately.
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- The second capture path. Fails independently of notifications. -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- Provisioning QR only. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".JommaApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jomma">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Provisioning by App Link.

                The QR is a plain `https://<host>/pair/<code>` URL, so *any* QR
                scanner will offer to open it — the camera app, a scanner the
                operator already has, whatever is on the phone. With
                android:autoVerify Android fetches
                `https://<host>/.well-known/assetlinks.json`, checks this app's
                signing certificate is named there, and then routes the link
                straight here: no chooser, no browser.

                That verification is also the security property. Since Android
                12 an app that cannot prove the domain endorses it does not get
                the link and cannot register for it either, so no other app can
                intercept a provisioning code. A custom `jomma://` scheme would
                have been simpler and would have had exactly the opposite
                property — any app may claim one.

                The host is a build input; see manifestPlaceholders in
                build.gradle.kts. Self-hosted software has no single domain to
                hardcode.

                Unverified, this filter still works — the link opens with a
                chooser rather than silently — and upgrades itself the moment
                assetlinks.json names the right fingerprint.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="${jommaHost}"
                    android:pathPrefix="/pair/" />
            </intent-filter>
        </activity>

        <!--
            The primary capture path. The user must grant notification access in
            system settings; there is no runtime permission for this.
        -->
        <service
            android:name=".capture.NotificationListener"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!--
            A foreground service is the strongest signal to Android that this
            process should stay alive. Ugly persistent notification is the point.
        -->
        <service
            android:name=".service.NotifierService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false" />

        <!--
            Re-arms the restart alarm and brings the service back if it is gone.
            A second recovery path, deliberately in a different subsystem from
            the WorkManager watchdog: vendor ROMs that defer background work
            tend not to defer alarms in the same way, so the two fail at
            different times instead of together.
        -->
        <receiver
            android:name=".service.RestartAlarm$Receiver"
            android:exported="false" />

        <!--
            Hands a downloaded APK to Android's installer. A file:// URI to
            another process throws FileUriExposedException on anything modern,
            and the installer is another process.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_paths" />
        </provider>

        <receiver
            android:name=".capture.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
